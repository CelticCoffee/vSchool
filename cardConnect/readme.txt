1) Read API document to become familiar with CardConnect's Hosted Payment Page
2) Open checkout.html and replace all instances of 'https://securepayments.prinpay.com' with location of your css files.
3) Use checkout.html as a test to become familiar with the API and how to embed it in your web application.